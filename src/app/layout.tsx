import type { Metadata } from "next";
import { Inter, Roboto_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
});

const robotoMono = Roboto_Mono({
  variable: "--font-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EXIF Viewer | Free Online Photo EXIF Data Viewer",
  description: "Free online tool to view and analyze EXIF data from your photos. Check camera settings, location data, and more from your images instantly.",
  keywords: "EXIF viewer, photo metadata, image EXIF data, online EXIF reader, photography data, camera settings",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${robotoMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
